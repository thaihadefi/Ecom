extends ../layouts/default.pug
include ../mixins/index.pug

block main
  .row
    .col-12
      .card
        .card-header
          +trash-header(`/${pathAdmin}/account-admin/list`)
        .card-body.pt-0
          +trash-buttons(`/${pathAdmin}/api/admin-accounts`)
          .table-responsive
            table.table.table-striped.mb-0
              thead.table-light
                tr
                  th(scope="col")
                    input.checkbox-all(type="checkbox" aria-label="Select all rows")
                  th(scope="col") No.
                  th(scope="col") Full Name
                  th(scope="col") Email
                  th(scope="col") Deleted At
                  th.text-end(scope="col") Action
              tbody
                if !recordList || !recordList.length
                  +empty-state("Trash is empty")
                else
                  each item, index in recordList
                    tr
                      td
                        input.checkbox-input(type="checkbox" aria-label="Select this row" value=item.id)
                      td #{index + 1}
                      td #{item.fullName}
                      td #{item.email}
                      td
                        small #{formatDateTime(item.deletedAt)}
                      td.text-end
                        +trash-action-buttons(
                          `/${pathAdmin}/api/admin-accounts/${item.id}/restore`,
                          `/${pathAdmin}/api/admin-accounts/${item.id}?permanent=true`
                        )
