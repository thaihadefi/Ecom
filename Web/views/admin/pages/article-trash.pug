extends ../layouts/default.pug
include ../mixins/index.pug

block main
  .row
    .col-12
      .card
        .card-header
          +trash-header(`/${pathAdmin}/article/list`)
        .card-body.pt-0
          +trash-buttons(`/${pathAdmin}/article/change-multi`)
          .table-responsive
            table.table.table-striped.mb-0
              thead.table-light
                tr
                  th(scope="col")
                    input.checkbox-all(type="checkbox")
                  th(scope="col") No.
                  th(scope="col") Avatar
                  th(scope="col") Article Title
                  th(scope="col") Deleted At
                  th.text-end(scope="col") Action
              tbody
                if !recordList || !recordList.length
                  +empty-state("Trash is empty")
                else
                  each item, index in recordList
                    tr
                      td
                        input.checkbox-input(type="checkbox" value=item.id)
                      td #{index + 1}
                      td
                        if item.avatar
                          img(src=`${domainCDN}${item.avatar}` class="rounded thumb-md" alt="" loading="lazy")
                      td #{item.name}
                      td
                        small #{formatDateTime(item.deletedAt)}
                      td.text-end
                        +trash-action-buttons(
                          `/${pathAdmin}/article/undo/${item.id}`,
                          `/${pathAdmin}/article/destroy/${item.id}`
                        )
