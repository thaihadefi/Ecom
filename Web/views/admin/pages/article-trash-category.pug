extends ../layouts/default.pug
include ../mixins/index.pug

block main
  .row
    .col-12
      .card
        .card-header
          +trash-header(`/${pathAdmin}/article/category`)
        .card-body.pt-0
          +trash-buttons(`/${pathAdmin}/article/category/change-multi`)
          .table-responsive
            table.table.table-striped.mb-0
              thead.table-light
                tr
                  th(scope="col")
                    input.checkbox-all(type="checkbox")
                  th(scope="col") No.
                  th(scope="col") Category Name
                  th(scope="col") Parent Category
                  th(scope="col") Status
                  th(scope="col") Deleted At
                  th.text-end(scope="col") Action
              tbody
                if !recordList || !recordList.length
                  +empty-state("Trash is empty")
                else
                  each item, index in recordList
                    tr
                      td
                        input.checkbox-input(type="checkbox" value=item.id)
                      td #{index + 1}
                      td #{item.name}
                      td #{item.parentName}
                      td
                        if (item.status == "active")
                          span.badge.bg-success Active
                        else if (item.status == "inactive")
                          span.badge.bg-danger Inactive
                      td
                        small #{formatDateTime(item.deletedAt)}
                      td.text-end
                        +trash-action-buttons(
                          `/${pathAdmin}/article/category/undo/${item.id}`,
                          `/${pathAdmin}/article/category/destroy/${item.id}`
                        )
